.Chat-main__message-list__info
  .Chat-main__message-list__info__message-name
    = message.user.name
  .Chat-main__message-list__info__qreated-at
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
.Chat-main__message-list__text-message
  - if message.content.present?
    %p.Message__content
      = message.content
  = image_tag message.image.url, class: 'Message__image' if message.image.present?
